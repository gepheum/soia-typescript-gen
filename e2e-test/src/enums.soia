enum Weekday {
  MONDAY;
  TUESDAY;
  WEDNESDAY;
  THURSDAY;
  FRIDAY;
  SATURDAY;
  SUNDAY;
}

enum JsonValue {
  NULL = 1;
  boolean: bool = 100;
  number: float64 = 20;
  string: string = 30;
  array: [JsonValue] = 40;
  struct Pair {
    name: string = 0;
    value: JsonValue = 1;
  }
  object: [Pair|name] = 50;
}

enum EmptyEnum {}

enum EnumWithNameConflict {
  SERIALIZER;
}
